generator client {
    provider = "prisma-client-js"
    // output       = "../client"
    // moduleFormat = "esm"
}

datasource db {
    provider = "postgresql"
    url      = env("KUN_DATABASE_URL")
}
